# Floo 平台项目总览与开发指引 (Master Project Context)

## 1. 项目愿景与定位
* **项目名称：** Floo
* **核心定位：** 一个基于 Web 的沉浸式互动悬疑解谜与剧情游戏平台。
* **设计意图：** 结合轻量级 ARG（侵入式虚拟现实游戏）元素与高智商心理博弈叙事（致敬《十四分之一》式局中局），通过独特的"壁炉传送"机制，让玩家在多个独立但暗中相连的悬疑时空中穿梭。
* **美学与氛围：** 暗黑、复古、神秘学、英伦高级感。拒绝低幼或千篇一律的网页小游戏质感，追求电影级的UI转场与沉浸式文本体验。

---

## 2. 技术栈标准
* **前端核心：** React (19+) + Vite + JavaScript/TypeScript
* **样式处理：** Tailwind CSS (配合自定义暗黑/复古魔法学院风色彩变量)
* **动画引擎：** Framer Motion（用于实现极致的页面切换、传送门火焰转场、卡片悬浮及解谜动效）
* **状态管理：** Zustand（用于管理全局大厅状态、跨游戏道具/背包"魔袋"、玩家通关进度）
* **音效管理：** Howler.js（用于处理环境白噪音、壁炉燃烧声、UI点击音效及BGM切换）

---

## 3. 架构设计与目录规范 (Monorepo 思想)
为了支持未来无限扩展多个不同题材的游戏,项目采用**平台大厅与剧本实例完全解耦**的架构：

```text
floo-matrix/
├── public/                     # 静态资源（全剧通用字体、音效、全局大厅贴图）
│   ├── audio/
│   └── images/
├── src/
│   ├── core/                   # 平台级核心底座 (Hub)
│   │   ├── components/         # 大厅全局组件（壁炉墙、转场动画、全局菜单、存档系统）
│   │   ├── store/              # 全局状态管理 (Zustand: 用户进度、已解锁壁炉、全局魔袋)
│   │   └── engine/             # 通用互动叙事引擎 (解析结构化JSON剧本树、分支、背包逻辑)
│   │
│   ├── games/                  # 游戏剧本独立插拔仓库 (Portal Instances)
│   │   ├── 01-escape-room/     # 示例游戏1（如克里斯局中局风格剧本）
│   │   │   ├── story.json      # 核心剧本树（对话、分支选项、条件触发）
│   │   │   ├── assets/         # 游戏专属立绘、BGM、解谜图片
│   │   │   └── components/     # 游戏专属UI组件（如密码盘、搜证拼图盘）
│   │   └── 02-next-game/       # 未来扩展的第二个游戏
│   │
│   ├── App.jsx                 # 根路由控制器（大厅 Hub 视图 vs 游戏实例 Portal 视图切换）
│   ├── main.jsx                # 入口文件
│   └── index.css               # 全局样式与 Tailwind 引入
├── package.json
├── tailwind.config.js
└── vite.config.js
```

## 4. 核心业务逻辑说明
**壁炉传送网 (The Floo Network)：**

* 玩家在主大厅看到的是一面"壁炉墙"。每个壁炉代表一个独立的解谜剧本。

* 点击壁炉时触发 Framer Motion 动效（屏幕产生视线拉近/火焰粒子放大效果），随后由核心叙事引擎加载对应 src/games/[game-id]/ 下的剧本。

**跨游戏记忆残留 (Memory Shards / Bag)：**

* 玩家在特定游戏中收集到的关键道具不单属于单局，而是存入全局的"Floo 魔袋"中，部分道具可用于点亮大厅彩蛋或解锁跨游戏的隐藏剧情。

**破壁与ARG彩蛋：**

* 代码结构需预留扩展接口，支持在特定剧情节点触发"网页异常报错"、"控制台F12线索注释"或"外部加密链接跳转"。

---

## 5. 开发原则与代码规范
**组件高内聚低耦合：** 尽量将UI表现与剧本数据（JSON）分离，通过通用引擎去渲染不同游戏。

**动效自然流畅：** 所有转场、弹窗、交互必须有过渡动画，严禁生硬的"硬切页"。

**代码可读性：** 保持逻辑清晰，注释规范，便于后续通过可视化后台或直接编写 JSON 快速扩展新游戏。
